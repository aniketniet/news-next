// Deprecated central NextAuth config. Logic moved directly into route file under
// app/api/auth/[...nextauth]/route.ts to avoid runtime handler destructuring errors.
// This file remains as a placeholder; you can safely delete it if not used elsewhere.
export {};
